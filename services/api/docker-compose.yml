services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: followup
      POSTGRES_PASSWORD: followup
      POSTGRES_DB: followup
    ports:
      - "5432:5432"
    volumes:
      - followup_pg:/var/lib/postgresql/data

volumes:
  followup_pg:
