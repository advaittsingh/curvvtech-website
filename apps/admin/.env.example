# Unified API origin — no trailing slash (Vite)
VITE_BACKEND_URL=http://localhost:3000
# Optional: api.curvvtech.in auto-strips /api from paths. Override with true/false if needed.
# VITE_STRIP_API_PREFIX=true
