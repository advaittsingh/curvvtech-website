# Unified API (same Postgres as services/api). Host only — no /api path, no trailing slash.
# Example production: https://api.curvvtech.in
NEXT_PUBLIC_API_URL=http://localhost:3000

# Required for public waitlist/onboarding routes (must match API LANDING_API_KEY)
NEXT_PUBLIC_LANDING_API_KEY=your-landing-key
